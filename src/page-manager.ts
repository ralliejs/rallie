class PageManager {
    static modules = [];

    constructor() {

    }

    static createModule (name) {
        console.log(name);
    }

    static loadModule () {
        
    }
}

export default PageManager;
